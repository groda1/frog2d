
- textures
- text rendering
- image render target
- console
- sort draw commands by pipeline to minimize rebinds
- investigate ASAN with arena. How to intercept and so on


# claude's notes

vulkan 1.4 features we now have access to but don't use yet:

- dynamic rendering (core 1.3): delete VkRenderPass and VkFramebuffer entirely;
  begin rendering with vkCmdBeginRendering straight to image views. Swapchain
  recreation shrinks (no framebuffer rebuild), and the image render target pass
  becomes much simpler to add. Biggest single cleanup available.
- synchronization2 (core 1.3): vkQueueSubmit2 + VkSemaphoreSubmitInfo give
  per-semaphore stage masks, and vkCmdPipelineBarrier2 puts stage+access in one
  struct. EndFrame's wait_semaphores/wait_stages arrays and the image layout
  transition barriers get clearer.
- timeline semaphores (core 1.2): one counter semaphore can replace the
  per-frame fence + binary semaphore sets. Also fixes the known debt where a
  failed frame leaves the transfer semaphore signaled (timeline waits don't
  consume signals).
- host image copy (core 1.4): vkCopyMemoryToImage uploads textures without a
  staging buffer, command buffer, or queue submit at all. Would replace most of
  VulkanImage_CreateStatic.
- push descriptors (core 1.4): alternative for the per-pipeline uniform sets
  (set 1) — no descriptor pools/sets to manage, just vkCmdPushDescriptorSet at
  bake time. Would delete the per-pipeline descriptor pool TODO.
- maintenance4+: pipeline layouts allow VK_NULL_HANDLE holes in set layouts,
  relaxed subgroup ops, etc. Minor conveniences.
- buffer device address (core 1.2): storage buffers without descriptors — pass
  vkGetBufferDeviceAddress as a 64-bit pointer in the push constant, shader
  dereferences it via GL_EXT_buffer_reference. Makes "growable SBOs" trivial:
  allocate bigger, pass the new address next frame, retire the old buffer
  after MAX_FRAMES_IN_FLIGHT frames. No descriptor rewrites, no pipeline
  tracking (the thing that made growable SBOs painful in vulkrap). Do before
  text rendering; start text with a fixed-cap SBO + overflow warning either way.
- per-frame GPU arena: instance data (text, sprites) is rewritten every frame,
  so it wants a transient allocator, not a persistent buffer per system — one
  buffer per frame-in-flight, sub-allocate ranges each frame (cursor reset in
  BeginFrame, like frame_arena on the cpu side), each draw gets base + offset
  via BDA push constant. Growth only happens at the arena level, at frame
  boundaries.

texture path follow-ups:

- texture uploads do 3 synchronous vkQueueWaitIdle submits each (transition,
  copy, transition); batch them into one single-time command buffer, or move
  uploads onto the per-frame transfer queue path (or host image copy, above)
- when the texture index moves from push constant to per-instance SSBO data,
  the array index becomes non-uniform: wrap with nonuniformEXT() in the shader
  (the device feature is already enabled)
- no mipmaps: sampler is clamped to maxLod 1.0 and upload creates 1 mip level;
  generate mips at upload (vkCmdBlitImage chain)
- sampler config is hardcoded (NEAREST mag / LINEAR min, repeat, aniso 16);
  expose filtering/addressing per sampler, e.g. sampler_config_t
- 2d_text_ssbo.frag's push constant layout is provisional (just textureIndex);
  settle it when text rendering is ported
- uniform buffers (set 1) could migrate into the global set model too; then
  every pipeline shares one layout and descriptor code exists in exactly one
  place

misc:

- route stb_image allocations through an arena (STBI_MALLOC/STBI_REALLOC/
  STBI_FREE macros, or File_Read + stbi_load_from_memory to at least keep file
  io in the arena)
- setup_physical_device picks the first 1.4-capable device; still needs the
  queue family / extension / feature / swapchain support checks from the TODO
  there, now including the Vulkan12Features descriptor indexing bits
- image.c is the only engine file including stb; keep it that way so the
  stb_inc meson include stays engine-private
