
- extern device, command_pool, graphics_queues and so on
- mesh shoudlnt ocntain vkbuffers and creating static buffers shouldnt return them either. abstract so we can remove vk from game
- uniforms
- resizing (pipeline recreation)
- textures
- text rendering
- image render target
- console
- sort draw commands by pipeline to minimize rebinds
- investigate ASAN with arena. How to intercept and so on
